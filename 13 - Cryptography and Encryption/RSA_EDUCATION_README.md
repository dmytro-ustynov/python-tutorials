# 📚 RSA Educational Materials

## 🎯 Для чого ці файли?

Набір матеріалів для вивчення RSA криптографії з використанням **маленьких простих чисел** (3259-3571) для освітніх цілей.

⚠️ **ВАЖЛИВО:** Ці прості числа використовуються ТІЛЬКИ для навчання! У реальному RSA використовуються прості числа з 1024+ біт (309+ цифр).

---

## 📁 Файли в наборі

### 1. `primes.py` - База простих чисел
**Що містить:**
- 40 простих чисел (3259-3571)
- Відповідають номерам студентів у журналі (1-40)
- Деякі приклади інших простих чисел для інформації

**Використання:**
```python
from primes import PRIMES
my_prime = PRIMES[15]  # Студент #15
```

### 2. `RSA_WORKSHEET.md` - Інструкція покроково ✏️
**Для кого:** Студенти, які хочуть зрозуміти RSA "руками"

**Що містить:**
- Покроковий worksheet з місцем для відповідей
- Пояснення кожного кроку RSA
- Приклади обчислень
- Завдання для практики
- Теоретичні питання

**Як використовувати:**
1. Відкрити файл
2. Заповнювати крок за кроком
3. Використовувати `rsa_calculator.py` для перевірки

### 3. `rsa_calculator.py` - Калькулятор для перевірки 🧮
**Для кого:** Студенти, які виконують worksheet

**Функції:**
- Обчислити n = p × q
- Обчислити φ(n)
- Перевірити gcd(e, φ(n))
- Знайти d (модульний обернений)
- Зашифрувати/розшифрувати числа
- Конвертувати текст ↔ ASCII коди
- Факторизувати n (демонстрація атаки)

**Запуск:**
```bash
python rsa_calculator.py
```

### 4. `rsa_educational.py` - Автоматична демонстрація 🎓
**Для кого:** Всі, хто хоче побачити повний процес RSA

**Що робить:**
- Інтерактивний вибір простих чисел (за номером або випадково)
- Автоматична генерація RSA ключів з поясненням
- Демонстрація шифрування чисел
- Демонстрація шифрування тексту
- Показує атаку факторизацією на маленькі n
- Пояснює чому маленькі прості небезпечні

**Запуск:**
```bash
python rsa_educational.py
```

---

## 🎓 Рекомендований порядок навчання

### Варіант 1: Самостійне вивчення (із worksheet)
1. ✅ Прочитати `RSA_WORKSHEET.md` (теорія)
2. ✅ Вибрати свої p та q з `primes.py`
3. ✅ Заповнити worksheet покроково
4. ✅ Використовувати `rsa_calculator.py` для перевірки кожного кроку
5. ✅ Запустити `rsa_educational.py` для повної автоматичної демонстрації

### Варіант 2: Швидке ознайомлення
1. ✅ Запустити `rsa_educational.py`
2. ✅ Експериментувати з різними простими числами
3. ✅ Прочитати `RSA_WORKSHEET.md` для деталей

### Варіант 3: Групова робота
1. ✅ Кожен студент вибирає свої p та q за номером
2. ✅ Вручну обчислюють ключі (worksheet)
3. ✅ Обмінюються публічними ключами
4. ✅ Шифрують повідомлення одне одному
5. ✅ Розшифровують отримані повідомлення

---

## 💡 Приклади використання

### Приклад 1: Студент #5 генерує ключі

```python
from primes import PRIMES

# Студент #5 вибирає своє просте
my_number = 5
p = PRIMES[my_number]  # 3541

# Вибрати q від іншого студента (наприклад, #10)
q = PRIMES[10]  # 3517

print(f"p = {p}")
print(f"q = {q}")
```

Далі обчислити:
- n = 3541 × 3517 = 12,453,697
- φ(n) = 3540 × 3516 = 12,446,640
- e = 65537 (або 3, 17)
- d = ? (використати калькулятор)

### Приклад 2: Шифрування повідомлення

```python
# Публічний ключ Bob: (e=65537, n=12,453,697)

message = 42
e = 65537
n = 12453697

ciphertext = pow(message, e, n)
print(f"Encrypted: {ciphertext}")

# Bob розшифровує своїм приватним ключем
d = 123456  # приклад (потрібно обчислити реальний)
decrypted = pow(ciphertext, d, n)
print(f"Decrypted: {decrypted}")
```

### Приклад 3: Атака факторизацією

```python
import time

n = 12453697  # маленький n

# Спроба факторизації
start = time.time()
for i in range(2, int(n**0.5) + 1):
    if n % i == 0:
        p = i
        q = n // i
        print(f"Зламано! p={p}, q={q}")
        break
elapsed = time.time() - start

print(f"Час атаки: {elapsed*1000:.2f} ms")
# Результат: < 1 мілісекунда! 🚨
```

---

## 🎯 Навчальні цілі

Після виконання вправ студент повинен:

✅ **Розуміти математику RSA:**
- Як обчислюється n, φ(n), e, d
- Чому e та φ(n) мають бути взаємно простими
- Що таке модульний обернений

✅ **Вміти:**
- Генерувати RSA ключі вручну
- Шифрувати та розшифровувати повідомлення
- Конвертувати текст у числа і назад

✅ **Розуміти безпеку:**
- Чому великі прості числа важливі
- Як працює атака факторизацією
- Різниця між навчальним та реальним RSA

---

## 📊 Порівняння: Навчальний vs Реальний RSA

| Параметр | Навчальний RSA | Реальний RSA |
|----------|----------------|--------------|
| **Розмір p, q** | ~3300 (~12 біт) | 1024+ біт |
| **Розмір n** | ~11 млн (24 біт) | 2048+ біт (617+ цифр) |
| **Час факторизації** | < 1 мс ⚠️ | Мільйони років ✅ |
| **Максимальне повідомлення** | < 11 млн | Величезне |
| **Безпечність** | ❌ НІ | ✅ ТАК |

---

## ⚠️ Застереження

### ❌ НЕ використовуйте цей код в продакшені!

**Чому:**
1. Маленькі прості числа → легко зламати
2. Немає padding (OAEP) → вразливий до атак
3. Навчальна реалізація → не оптимізована

### ✅ Що використовувати в реальних проектах:

```python
# Використовуйте cryptography library!
from cryptography.hazmat.primitives.asymmetric import rsa

# Генерувати РЕАЛЬНІ RSA ключі
private_key = rsa.generate_private_key(
    public_exponent=65537,
    key_size=2048  # або 4096
)
```

---

## 🧪 Тестові сценарії

### Сценарій 1: Базовий
1. Вибрати p=3541, q=3517
2. Обчислити n, φ(n), e, d
3. Зашифрувати число 100
4. Розшифрувати назад

### Сценарій 2: Обмін повідомленнями
1. Alice (студент #5): p=3541, q=3517
2. Bob (студент #10): p=3517, q=3499
3. Alice шифрує "HI" для Bob (використовує публічний ключ Bob)
4. Bob розшифровує (використовує свій приватний ключ)

### Сценарій 3: Атака
1. Зламати n=12,453,697 факторизацією
2. Виміряти час
3. Порівняти з часом для більшого n

---

## 📚 Додаткові матеріали

### В цій папці також є:
- `ALGORITHMS_EXPLAINED.md` - RSA vs ED25519 vs X25519
- `SSH_EXPLAINED.md` - Як ED25519 використовується в SSH
- `comparison_rsa_vs_ed25519.py` - Порівняння алгоритмів

### Корисні посилання:
- [RSA Wikipedia](https://uk.wikipedia.org/wiki/RSA)
- [Khan Academy: RSA Encryption](https://www.khanacademy.org/computing/computer-science/cryptography/modern-crypt/v/intro-to-rsa-encryption)
- [Computerphile: RSA](https://www.youtube.com/watch?v=wXB-V_Keiu8)

---

## 🎓 Для викладачів

### Рекомендації:

1. **Час на виконання:**
   - Worksheet: 45-60 хвилин
   - Практика з калькулятором: 20-30 хвилин
   - Групова робота: 60-90 хвилин

2. **Рівень складності:**
   - Потрібні базові знання: модульна арифметика, прості числа
   - Не потрібні: складна математика, програмування (для worksheet)

3. **Оцінювання:**
   - Перевірити worksheet на правильність обчислень
   - Практична вправа: обмін зашифрованими повідомленнями
   - Теоретичні питання в кінці worksheet

4. **Розширення:**
   - Порівняти з іншими алгоритмами (ED25519, X25519)
   - Обговорити історію криптографії
   - Показати реальні застосування RSA

---

**Гарного навчання! 🎓🔐**

Питання? Звертайтесь до викладача або дивіться документацію в README.md