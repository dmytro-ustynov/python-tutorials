# 🔐 Пояснення: RSA vs ED25519 vs X25519

## ❓ Чому ED25519 не може шифрувати?

### Коротка відповідь
**ED25519** = алгоритм **підпису**, а не шифрування. Математично він створений тільки для підписів.

### Довша відповідь

Існує два різних типи асиметричних ключів на основі Curve25519 (і один класичний алгоритм для порівняння):

| Алгоритм | Основа | Призначення | Може шифрувати? | Може підписувати? |
|----------|--------|-------------|-----------------|-------------------|
| **ED25519** | Curve25519 | Digital signatures (EdDSA) | ❌ НІ | ✅ ТАК |
| **X25519** | Curve25519 | Key exchange (ECDH) | ✅ ТАК* | ❌ НІ |
| **RSA** | Факторизація чисел | Universal | ✅ ТАК | ✅ ТАК |

_* X25519 технічно робить "key exchange", а не пряме шифрування_

---

## 🔑 Curve25519 - одна крива, два алгоритми

**Curve25519** - це еліптична крива, розроблена Daniel J. Bernstein.

На її основі створено ДВА різних алгоритми:

### 1️⃣ ED25519 (EdDSA) - для підписів
```python
from cryptography.hazmat.primitives.asymmetric import ed25519

# Генерувати ключі для підписів
private_key = ed25519.Ed25519PrivateKey.generate()
public_key = private_key.public_key()

# ✅ Підпис працює
signature = private_key.sign(b"message")
public_key.verify(signature, b"message")

# ❌ Шифрування НЕ працює - немає таких методів!
# encrypted = public_key.encrypt(b"message")  # AttributeError!
```

**Чому так?** Математичні операції EdDSA оптимізовані для створення та перевірки підписів, а не для шифрування.

### 2️⃣ X25519 (ECDH) - для обміну ключами
```python
from cryptography.hazmat.primitives.asymmetric import x25519

# Генерувати ключі для key exchange
private_key = x25519.X25519PrivateKey.generate()
public_key = private_key.public_key()

# ✅ Key exchange працює
shared_secret = private_key.exchange(other_public_key)

# ❌ Підпис НЕ працює - немає таких методів!
# signature = private_key.sign(b"message")  # AttributeError!
```

**Як це працює?** X25519 використовує Diffie-Hellman key exchange на еліптичній кривій:
1. Alice та Bob обмінюються публічними ключами
2. Обидва обчислюють однаковий **shared secret**
3. Цей shared secret використовується для симетричного шифрування (наприклад, Fernet)

---

## 📊 Порівняння

### Розмір ключів
```
RSA-2048 приватний ключ:  ~1700 bytes
RSA-2048 публічний ключ:   ~450 bytes

ED25519 приватний ключ:    ~120 bytes  ⬇️ 14x менше!
ED25519 публічний ключ:    ~80 bytes   ⬇️ 5x менше!

X25519 приватний ключ:     ~120 bytes
X25519 публічний ключ:     ~80 bytes
```

### Розмір підпису
```
RSA-2048 підпис:  256 bytes
ED25519 підпис:   64 bytes   ⬇️ 4x менше!
```

### Швидкість
```
Key generation:
  ED25519:  ~0.5ms   ⚡⚡⚡
  X25519:   ~0.5ms   ⚡⚡⚡
  RSA-2048: ~50ms    ⚡ (100x повільніше!)

Signing:
  ED25519:  ~0.05ms  ⚡⚡⚡
  RSA-2048: ~1ms     ⚡⚡ (20x повільніше!)

Verification:
  ED25519:  ~0.1ms   ⚡⚡⚡
  RSA-2048: ~0.05ms  ⚡⚡⚡ (RSA verify швидкий!)
```

---

## 🎯 Що використовувати?

### Для підписів → ED25519
```python
# ✅ Найкраще для digital signatures
from cryptography.hazmat.primitives.asymmetric import ed25519

private_key = ed25519.Ed25519PrivateKey.generate()
signature = private_key.sign(message)
```

**Переваги:**
- ⚡ Дуже швидкий
- 📦 Маленькі ключі та підписи
- 🔒 Безпечний (256-bit security)

**Використання:**
- Git commit signing
- SSH authentication
- JWT signing
- API signatures

### Для шифрування → X25519 + Fernet (hybrid)
```python
# ✅ Сучасний підхід: ECDH + symmetric encryption
from cryptography.hazmat.primitives.asymmetric import x25519
from cryptography.fernet import Fernet

# Key exchange
alice_private = x25519.X25519PrivateKey.generate()
bob_public = # отримати від Bob

shared_secret = alice_private.exchange(bob_public)
# Конвертувати shared_secret в Fernet key
encryption_key = derive_key(shared_secret)

# Шифрувати
cipher = Fernet(encryption_key)
encrypted = cipher.encrypt(message)
```

**Переваги:**
- ⚡ Дуже швидкий
- 📦 Маленькі ключі
- 🔒 Perfect forward secrecy

**Використання:**
- Signal/WhatsApp (протокол Double Ratchet)
- TLS 1.3
- VPN (WireGuard)
- Secure messaging

### Для універсального використання → RSA
```python
# ✅ Працює для всього, але великий і повільний
from cryptography.hazmat.primitives.asymmetric import rsa

private_key = rsa.generate_private_key(
    public_exponent=65537,
    key_size=2048
)

# Може і шифрувати і підписувати
encrypted = public_key.encrypt(message, padding)
signature = private_key.sign(message, padding, hash_algo)
```

**Переваги:**
- 📚 Широка підтримка (legacy systems)
- 🔒 Перевірений часом
- 🛠️ Один ключ для всього

**Недоліки:**
- 🐌 Повільний
- 📦 Великі ключі

**Використання:**
- Legacy systems
- PGP/GPG email encryption
- SSL/TLS (старіші версії)
- Коли потрібна сумісність

---

## 💡 Best Practice: Комбінувати обидва!

Сучасні криптографічні системи використовують **обидва** алгоритми:

```python
# 1️⃣ X25519 для шифрування
encrypted_message = encrypt_with_x25519(message, recipient_public_key)

# 2️⃣ ED25519 для підпису
signature = sign_with_ed25519(encrypted_message, sender_private_key)

# Надіслати:
# - encrypted_message
# - signature
# - sender_public_key (для verify)
# - ephemeral_public_key (для decrypt)
```

Це дає:
- 🔒 **Confidentiality** (X25519 encryption)
- ✍️ **Authenticity** (ED25519 signature)
- 🛡️ **Integrity** (підпис виявить модифікації)

**Приклади:**
- Signal/WhatsApp protocol
- Age encryption tool
- Modern SSH

---

## 📝 Запам'ятайте

```
┌─────────────────────────────────────────────────┐
│  Треба зашифрувати?    → X25519 + Fernet       │
│  Треба підписати?      → ED25519                │
│  Треба обидва?         → X25519 + ED25519       │
│  Legacy сумісність?    → RSA                    │
└─────────────────────────────────────────────────┘
```

## 🏃 Запустіть приклади

```bash
# 1. Генерувати всі ключі
python generate_keys.py

# 2. Порівняння алгоритмів
python comparison_rsa_vs_ed25519.py

# 3. RSA шифрування
python encrypt_with_rsa.py

# 4. ED25519 підписи
python sign_with_ed25519.py

# 5. X25519 hybrid encryption
python hybrid_encryption_x25519.py
```

---

## 📚 Додаткові ресурси

- [ED25519 specification](https://ed25519.cr.yp.to/)
- [X25519 RFC 7748](https://datatracker.ietf.org/doc/html/rfc7748)
- [Age encryption tool](https://github.com/FiloSottile/age) - uses X25519
- [Signal Protocol](https://signal.org/docs/) - uses both X25519 and ED25519

---

**Висновок:** ED25519 не може шифрувати, бо це алгоритм підпису. Для шифрування використовуйте X25519 (або RSA для legacy). Краще всього - комбінувати обидва! 🔐✍️