4# 📝 RSA Cryptography - Step-by-Step Worksheet

## 🎯 Мета: Зрозуміти як працює RSA шифрування "руками"

⚠️ **Важливо:** Ми використовуємо маленькі прості числа для навчання. У реальному RSA використовуються ВЕЛИЧЕЗНІ прості числа (2048+ біт)!

---

## 📋 Частина 1: Вибір простих чисел

### Крок 1: Знайдіть свої прості числа

Використайте таблицю простих чисел (`primes.py`):

| Номер студента | Просте число | Номер студента | Просте число |
|----------------|--------------|----------------|--------------|
| 1 | 3571 | 21 | 3413 |
| 2 | 3559 | 22 | 3407 |
| 3 | 3557 | 23 | 3391 |
| 4 | 3547 | 24 | 3389 |
| 5 | 3541 | 25 | 3373 |
| 6 | 3539 | 26 | 3371 |
| 7 | 3533 | 27 | 3361 |
| 8 | 3529 | 28 | 3359 |
| 9 | 3527 | 29 | 3347 |
| 10 | 3517 | 30 | 3343 |
| 11 | 3511 | 31 | 3331 |
| 12 | 3499 | 32 | 3329 |
| 13 | 3491 | 33 | 3323 |
| 14 | 3469 | 34 | 3319 |
| 15 | 3467 | 35 | 3313 |
| 16 | 3463 | 36 | 3307 |
| 17 | 3461 | 37 | 3301 |
| 18 | 3457 | 38 | 3299 |
| 19 | 3449 | 39 | 3271 |
| 20 | 3433 | 40 | 3259 |

**Ваш вибір:**
```
p = _________ (ваш номер студента)
q = _________ (номер сусіда або випадковий інший номер)
```

---

## 🔑 Частина 2: Генерація RSA ключів

### Крок 2: Обчислити n

Формула: `n = p × q`

```
n = _______ × _______ = _____________
```

**Що таке n?**
- n — це модуль, який використовується в публічному та приватному ключі
- Довжина n (в бітах) визначає "силу" RSA
- У нашому прикладі: n ≈ 11 млн (24 біти)
- У реальному RSA: n має 2048+ біт!

---

### Крок 3: Обчислити φ(n) - функцію Ейлера

Формула: `φ(n) = (p - 1) × (q - 1)`

```
φ(n) = (_______ - 1) × (_______ - 1)
φ(n) = _______ × _______
φ(n) = _____________
```

**Що таке φ(n)?**
- φ(n) = кількість чисел менших за n, які є взаємно простими з n
- Для добутку двох простих: φ(p×q) = (p-1)×(q-1)
- Це "секретне" число, яке не повинно бути відоме

---

### Крок 4: Вибрати публічну експоненту e

**Умови для e:**
1. `1 < e < φ(n)`
2. `gcd(e, φ(n)) = 1` (e та φ(n) взаємно прості)

**Популярні значення e:**
- 3 (швидке, але менш безпечне)
- 17
- 65537 (найпопулярніше, F4)

**Спробуйте e = 65537:**

Перевірка: `gcd(65537, φ(n)) = ?`

Якщо gcd ≠ 1, спробуйте e = 17 або e = 3

```
e = _______
```

**Примітка:** Можна використати онлайн калькулятор GCD або Python:
```python
import math
math.gcd(65537, phi)
```

---

### Крок 5: Обчислити приватну експоненту d

Формула: `d × e ≡ 1 (mod φ(n))`

Іншими словами: знайти d таке що `(d × e) mod φ(n) = 1`

**Це називається "модульний обернений"**

Для обчислення використайте розширений алгоритм Евкліда або Python:

```python
def mod_inverse(e, phi):
    def extended_gcd(a, b):
        if a == 0:
            return b, 0, 1
        gcd, x1, y1 = extended_gcd(b % a, a)
        x = y1 - (b // a) * x1
        y = x1
        return gcd, x, y

    gcd, x, y = extended_gcd(e, phi)
    if gcd != 1:
        raise ValueError("Модульний обернений не існує")
    return x % phi

d = mod_inverse(e, phi)
```

```
d = _____________
```

**Перевірка:** `(e × d) mod φ(n) = ?` (має бути 1)

```
(_______ × _______) mod _______ = _______
```

---

## 📢 Частина 3: Ваші ключі

### Публічний ключ (можна ділитися)
```
Public Key = (e, n) = (_______, _______)
```

### Приватний ключ (зберігати в секреті!)
```
Private Key = (d, n) = (_______, _______)
```

---

## 🔐 Частина 4: Шифрування повідомлення

### Формула шифрування:
```
Ciphertext = (Message^e) mod n
```

### Приклад: Зашифрувати число 100

```
Message = 100
Ciphertext = (100^e) mod n
Ciphertext = (100^_____) mod _______
Ciphertext = _____________
```

**Використайте Python для обчислення:**
```python
message = 100
ciphertext = pow(message, e, n)  # Швидке модульне піднесення до степеня
```

---

## 🔓 Частина 5: Розшифрування повідомлення

### Формула розшифрування:
```
Message = (Ciphertext^d) mod n
```

### Розшифрувати ваше повідомлення:

```
Ciphertext = _____________ (з попереднього кроку)
Message = (Ciphertext^d) mod n
Message = (_______^_____) mod _______
Message = _____________
```

**Використайте Python:**
```python
decrypted = pow(ciphertext, d, n)
```

**Перевірка:** Розшифроване повідомлення має дорівнювати оригінальному (100)

```
Decrypted = _______ ✅/❌
```

---

## 📝 Частина 6: Шифрування тексту

Щоб зашифрувати текст, шифруємо кожен символ окремо:

### Приклад: Зашифрувати "HI"

**Крок 1:** Конвертувати символи в числа (ASCII коди)
```
'H' → 72
'I' → 73
```

**Крок 2:** Зашифрувати кожне число
```
C1 = (72^e) mod n = _____________
C2 = (73^e) mod n = _____________
```

**Крок 3:** Зашифрований текст = список чисел
```
Encrypted = [C1, C2] = [_______, _______]
```

### Розшифрування:

**Крок 1:** Розшифрувати кожне число
```
M1 = (C1^d) mod n = _____________
M2 = (C2^d) mod n = _____________
```

**Крок 2:** Конвертувати числа назад в символи
```
M1 = _______ → '_____'
M2 = _______ → '_____'
```

**Крок 3:** Отримати текст
```
Decrypted text = "_________"
```

---

## ⚠️ Частина 7: Чому маленькі прості небезпечні?

### Атака: Факторизація n

Якщо n маленьке, його легко розкласти на множники!

**Ваше n:** `n = _____________`

**Спроба факторизації:**

```python
import time

n = YOUR_N
start = time.time()

for i in range(2, int(n**0.5) + 1):
    if n % i == 0:
        p_found = i
        q_found = n // i
        print(f"Found: p = {p_found}, q = {q_found}")
        break

elapsed = time.time() - start
print(f"Time: {elapsed*1000:.2f} ms")
```

**Результат:**
```
Found: p = _______, q = _______
Time: _______ ms
```

### Чому це проблема?

Якщо хакер знає p та q, він може:
1. Обчислити φ(n) = (p-1)×(q-1)
2. Знайти d використовуючи e та φ(n)
3. Розшифрувати ВСІ повідомлення!

### Реальний RSA:

```
Ваше n:      ~11,000,000 (24 біти)
Час атаки:   < 1 мілісекунда ⚠️

Real RSA n:  617+ цифр (2048 біт)
Час атаки:   МІЛЬЙОНИ років ✅
```

---

## 🎯 Завдання для практики

### Завдання 1: Базове шифрування
1. Виберіть свої p та q
2. Обчисліть n, φ(n), e, d
3. Зашифруйте число 42
4. Розшифруйте його назад

### Завдання 2: Шифрування тексту
1. Зашифруйте своє ім'я (кирилицею або латиницею)
2. Обміняйтесь зашифрованим повідомленням з сусідом
3. Розшифруйте його повідомлення використовуючи свій приватний ключ

### Завдання 3: Атака
1. Обчисліть час факторизації вашого n
2. Порівняйте з часом для більшого n (наприклад, n двох найбільших простих)

### Завдання 4: Обмеження
1. Спробуйте зашифрувати число більше за n — що станеться?
2. Чому максимальне повідомлення має бути < n?

---

## 💻 Використання скрипта

Для автоматичної перевірки використайте:

```bash
python rsa_educational.py
```

Цей скрипт:
- Автоматично генерує ключі
- Демонструє шифрування/розшифрування
- Показує атаку факторизацією
- Пояснює кожен крок

---

## 📚 Теоретичні питання

1. **Чому RSA називається "асиметричним" шифруванням?**
   - [ ] Використовує різні ключі для шифрування та розшифрування
   - [ ] Шифрує дані асиметрично
   - [ ] Ключі різної довжини

2. **Що станеться якщо e та φ(n) не взаємно прості (gcd ≠ 1)?**
   - [ ] RSA все одно працюватиме
   - [ ] Неможливо знайти d (модульний обернений не існує)
   - [ ] Ключі будуть слабкими

3. **Чому φ(n) має бути секретним?**
   - [ ] Якщо відомо φ(n), можна знайти d
   - [ ] Якщо відомо φ(n), можна факторизувати n
   - [ ] φ(n) використовується для шифрування

4. **Яке обмеження на розмір повідомлення в RSA?**
   - [ ] Message < e
   - [ ] Message < n
   - [ ] Message < d

**Відповіді:** 1-а, 2-б, 3-а, 4-б

---

## ✅ Checklist виконання

- [ ] Вибрали свої p та q
- [ ] Обчислили n
- [ ] Обчислили φ(n)
- [ ] Вибрали e та перевірили gcd(e, φ(n)) = 1
- [ ] Обчислили d
- [ ] Зашифрували та розшифрували число
- [ ] Зашифрували та розшифрували текст
- [ ] Виміряли час факторизації
- [ ] Зрозуміли чому маленькі прості небезпечні
- [ ] Запустили `rsa_educational.py`

---

**Важливо:** Це навчальний приклад. У реальних додатках:
- Використовуйте криптографічні бібліотеки (cryptography, pycryptodome)
- НІКОЛИ не реалізовуйте RSA самостійно
- Використовуйте ключі 2048+ біт
- Додавайте padding (OAEP) для безпеки

🎓 **Гарного навчання!**