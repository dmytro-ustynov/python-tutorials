# 📝 ЗАВДАННЯ ДО ЗАНЯТТЯ 5.4: Моніторинг системних процесів

## Загальні вимоги

- Всі скрипти повинні мати docstrings
- Використовувати type hints де це можливо
- Обробляти помилки належним чином
- Код має бути читабельним та добре структурованим
- Додавати коментарі до складних частин коду

---

## 🟢 РІВЕНЬ 1: Базові завдання (по 1 балу)

### Завдання 1.1: Простий монітор CPU

Створіть скрипт `cpu_monitor.py`, який:
- Виводить поточне завантаження CPU
- Показує завантаження кожного ядра окремо
- Якщо завантаження будь-якого ядра > 80%, виводить попередження

**Приклад виводу:**
```
CPU Загальне: 45.2%
Ядро 0: 52.1%
Ядро 1: 38.3%
Ядро 2: 89.4% ⚠️ ПОПЕРЕДЖЕННЯ!
Ядро 3: 41.2%
```

**Підказка:** Використовуйте `psutil.cpu_percent(interval=1, percpu=True)`

---

### Завдання 1.2: Інформація про пам'ять

Створіть скрипт `memory_info.py`, який:
- Виводить загальну, використану та вільну пам'ять в GB
- Показує відсоток використання
- Виводить інформацію про swap (якщо є)
- Використовує кольорове виводження (зелений < 70%, жовтий 70-90%, червоний > 90%)

**Підказка:** Використовуйте ANSI escape коди для кольорів або бібліотеку `colorama`

---

### Завдання 1.3: Список процесів

Створіть скрипт `list_processes.py`, який:
- Виводить список всіх запущених процесів
- Для кожного процесу показує: PID, ім'я, користувач
- Сортує процеси за PID
- Дозволяє фільтрувати за іменем користувача через аргумент командного рядка

**Приклад запуску:**
```bash
python list_processes.py
python list_processes.py --user dmytroustynov
```

---

### Завдання 1.4: Інформація про диски

Створіть скрипт `disk_info.py`, який:
- Виводить інформацію про всі підключені диски
- Для кожного диска показує: точку монтування, загальний розмір, використано, вільно
- Виділяє диски з використанням > 90%
- Підраховує загальний об'єм всіх дисків

**Бонус:** Додайте візуальну шкалу заповнення (progress bar)

```
/       [████████░░] 80.5% (150.2 GB / 186.5 GB)
/home   [██████████] 95.2% (285.6 GB / 300.0 GB) ⚠️
```

---

### Завдання 1.5: Пошук процесу за іменем

Створіть скрипт `find_process.py`, який:
- Приймає ім'я процесу як аргумент командного рядка
- Знаходить всі процеси з таким іменем
- Виводить детальну інформацію про кожен знайдений процес (PID, CPU, пам'ять)

**Приклад:**
```bash
python find_process.py python
```

**Вивід:**
```
Знайдено 3 процеси "python":

PID: 1234
  Ім'я: python3
  CPU: 2.5%
  Пам'ять: 45.2 MB
  Час запуску: 2024-11-21 10:30:15

...
```

---

## 🟡 РІВЕНЬ 2: Середні завдання (по 2 бали)

### Завдання 2.1: Топ процесів

Створіть скрипт `top_processes.py`, який імітує команду `top`:
- Виводить топ-10 процесів за CPU
- Виводить топ-10 процесів за пам'яттю
- Оновлює дані кожні 2 секунди (використовуйте `time.sleep` та очищення екрану)
- Дозволяє вибрати критерій сортування через аргумент (`--sort cpu` або `--sort memory`)
- Дозволяє вказати кількість процесів (`--top 5`, `--top 20`)

**Приклад:**
```bash
python top_processes.py --sort cpu --top 5
```

**Підказка:** Для очищення екрану використовуйте `os.system('clear')` (Linux/Mac) або `os.system('cls')` (Windows)

---

### Завдання 2.2: Звіт у форматі JSON

Створіть скрипт `system_report_json.py`, який:
- Збирає інформацію про систему (CPU, пам'ять, диски, мережу)
- Збирає топ-5 процесів за CPU та пам'яттю
- Зберігає все в JSON файл з міткою часу в імені
- Має структуровану організацію даних

**Структура JSON:**
```json
{
  "timestamp": "2024-11-21T14:30:45",
  "system": {
    "cpu": {...},
    "memory": {...},
    "disk": {...},
    "network": {...}
  },
  "top_processes": {
    "by_cpu": [...],
    "by_memory": [...]
  }
}
```

---

### Завдання 2.3: Моніторинг з історією

Створіть скрипт `continuous_monitor.py`, який:
- Збирає метрики CPU та пам'яті кожні N секунд (параметр `--interval`)
- Зберігає дані в CSV файл
- Додає нові записи до існуючого файлу (не перезаписує)
- Працює в циклі, виводить поточні дані на екран
- Можна зупинити через Ctrl+C (обробка KeyboardInterrupt)

**CSV формат:**
```csv
timestamp,cpu_percent,memory_percent,memory_used_gb
2024-11-21 14:30:00,45.2,67.8,10.8
2024-11-21 14:30:30,52.1,68.2,10.9
...
```

**Приклад запуску:**
```bash
python continuous_monitor.py --interval 30 --output monitoring.csv
```

---

### Завдання 2.4: Single Instance через PID файл

Створіть скрипт `single_instance_demo.py`, який:
- Реалізує клас `SingleInstance` з використанням PID файлу
- Запобігає запуску більше одного екземпляру
- При спробі повторного запуску виводить інформаційне повідомлення
- Коректно очищує lock файл при виході
- Обробляє ситуацію, коли lock файл залишився від завершеного процесу

**Тестування:**
1. Запустіть скрипт в одному терміналі
2. Спробуйте запустити ще раз в іншому терміналі
3. Перший запуск має продовжувати роботу, другий має завершитись з повідомленням

---

### Завдання 2.5: Аргументи з валідацією

Створіть скрипт `validated_monitor.py` з argparse, який має:
- Аргумент `--cpu-threshold` (float, 0-100)
- Аргумент `--memory-threshold` (float, 0-100)
- Аргумент `--interval` (int, мінімум 1)
- Аргумент `--output` (файл, батьківська директорія має існувати)
- Валідацію всіх аргументів через власні функції
- Докладне повідомлення про помилку при невалідних даних

**Приклад використання:**
```bash
python validated_monitor.py --cpu-threshold 80 --memory-threshold 90 --interval 60
python validated_monitor.py --cpu-threshold 150  # Помилка: має бути 0-100
```

---

## 🔴 РІВЕНЬ 3: Складні завдання (по 3 бали)

### Завдання 3.1: Повноцінна система моніторингу

Створіть скрипт `advanced_monitor.py`, який поєднує ВСІ вивчені концепції:

**Функціональність:**
1. **Single Instance:** Використання PID файлу для запобігання дублювання
2. **Конфігурація через argparse:**
   - `--cpu-threshold` - поріг CPU (за замовчуванням 80)
   - `--memory-threshold` - поріг пам'яті (за замовчуванням 90)
   - `--disk-threshold` - поріг диска (за замовчуванням 90)
   - `--interval` - інтервал між перевірками (за замовчуванням 60)
   - `--output` - директорія для звітів (за замовчуванням './reports')
   - `--format` - формат звіту: json, csv, text (за замовчуванням json)
   - `--continuous` - безперервний режим (прапорець)
   - `--verbose` - детальний вивід (прапорець)

3. **Моніторинг:**
   - CPU (загальний та по ядрах)
   - Пам'ять (RAM та Swap)
   - Диски (всі підключені)
   - Мережа (трафік)
   - Топ-5 процесів за CPU та пам'яттю

4. **Попередження:**
   - Перевірка порогів для всіх параметрів
   - Виведення попереджень в консоль та збереження в лог

5. **Звіти:**
   - Збереження в обраному форматі
   - Імена файлів з мітками часу
   - Структуровані дані

6. **Режими роботи:**
   - One-shot: зібрати дані один раз та вийти
   - Continuous: працювати в циклі з вказаним інтервалом

**Приклад використання:**
```bash
# One-shot режим
python advanced_monitor.py --format json --verbose

# Continuous режим
python advanced_monitor.py --continuous --interval 30 --format csv

# З налаштованими порогами
python advanced_monitor.py --cpu-threshold 70 --memory-threshold 85 --disk-threshold 95
```

**Критерії оцінювання:**
- ✅ Single Instance працює коректно (0.5 бали)
- ✅ Всі аргументи з валідацією (0.5 бали)
- ✅ Збір всіх метрик (0.5 бали)
- ✅ Система попереджень (0.5 бали)
- ✅ Генерація звітів у всіх форматах (0.5 бали)
- ✅ Обидва режими роботи (0.5 бали)

---

### Завдання 3.2: Аналізатор процесів

Створіть скрипт `process_analyzer.py` з підкомандами:

**Підкоманди:**

1. **list** - список всіх процесів
   ```bash
   python process_analyzer.py list
   python process_analyzer.py list --sort cpu
   python process_analyzer.py list --sort memory --top 10
   ```

2. **find** - пошук процесів
   ```bash
   python process_analyzer.py find python
   python process_analyzer.py find chrome --details
   ```

3. **kill** - зупинка процесу
   ```bash
   python process_analyzer.py kill 1234
   python process_analyzer.py kill 1234 --force
   ```

4. **monitor** - моніторинг конкретного процесу
   ```bash
   python process_analyzer.py monitor 1234 --interval 5
   ```

5. **tree** - дерево процесів (батько-нащадки)
   ```bash
   python process_analyzer.py tree
   python process_analyzer.py tree --pid 1234
   ```

**Вимоги:**
- Кожна підкоманда має свої власні аргументи
- Обробка помилок (процес не існує, немає прав, тощо)
- Інформативні повідомлення про помилки
- Довідка для кожної підкоманди

---

### Завдання 3.3: Система сповіщень

Створіть скрипт `alert_monitor.py`, який:

**Функціональність:**
1. Моніторить систему з заданим інтервалом
2. При перевищенні порогів:
   - Виводить попередження в консоль
   - Зберігає в лог файл
   - **Відправляє email** (використовуйте `smtplib`)
   - Зберігає звіт про інцидент

3. Конфігурація через JSON файл:
```json
{
  "thresholds": {
    "cpu": 80,
    "memory": 90,
    "disk": 95
  },
  "email": {
    "enabled": true,
    "smtp_server": "smtp.gmail.com",
    "smtp_port": 587,
    "from": "monitoring@example.com",
    "to": ["admin@example.com"],
    "subject": "System Alert"
  },
  "monitoring": {
    "interval": 60,
    "log_file": "/var/log/system_monitor.log"
  }
}
```

4. Запобігання спаму:
   - Не відправляти більше 1 email на 10 хвилин для одного типу попередження
   - Групувати кілька проблем в один email

**Додаткові функції:**
- Ротація лог файлів (max 10 MB, зберігати останні 5 файлів)
- Статистика (скільки разів було перевищено поріг, середні значення)
- Web dashboard (опціонально, використовуйте Flask)

---

### Завдання 3.4: Історична аналітика

Створіть скрипт `analytics.py`, який:

1. **Збір даних:**
   - Збирає метрики кожні N секунд
   - Зберігає в SQLite базу даних
   - Таблиці: system_metrics, process_metrics, alerts

2. **Аналіз даних:**
   - Підкоманда `stats` - статистика за період:
     ```bash
     python analytics.py stats --from "2024-11-20" --to "2024-11-21"
     python analytics.py stats --last-hour
     python analytics.py stats --last-day
     ```

   - Виводить:
     - Середнє, мінімальне, максимальне завантаження CPU
     - Пікові навантаження
     - Кількість попереджень
     - Процеси, які найбільше використовували ресурси

3. **Візуалізація:**
   - Підкоманда `plot` - графіки:
     ```bash
     python analytics.py plot --metric cpu --last-day
     python analytics.py plot --metric memory --output graph.png
     ```

   - Використовуйте `matplotlib` для графіків
   - Збереження в PNG файли

4. **Експорт:**
   - Підкоманда `export`:
     ```bash
     python analytics.py export --format csv --output report.csv
     python analytics.py export --format json --from "2024-11-20"
     ```

**База даних (SQLite):**
```sql
CREATE TABLE system_metrics (
    id INTEGER PRIMARY KEY,
    timestamp DATETIME,
    cpu_percent REAL,
    memory_percent REAL,
    disk_percent REAL,
    network_sent_mb REAL,
    network_recv_mb REAL
);

CREATE TABLE process_metrics (
    id INTEGER PRIMARY KEY,
    timestamp DATETIME,
    pid INTEGER,
    name TEXT,
    cpu_percent REAL,
    memory_percent REAL
);

CREATE TABLE alerts (
    id INTEGER PRIMARY KEY,
    timestamp DATETIME,
    alert_type TEXT,
    message TEXT,
    value REAL,
    threshold REAL
);
```

---

### Завдання 3.5: Розподілена система моніторингу

**Складне завдання для амбітних студентів!**

Створіть систему моніторингу з клієнт-серверною архітектурою:

**Компоненти:**

1. **Агент (`agent.py`)** - запускається на моніторених машинах:
   - Збирає метрики локальної системи
   - Відправляє дані на сервер через HTTP API
   - Single Instance
   - Конфігурація через JSON
   - Працює як daemon/service

2. **Сервер (`server.py`)** - централізований збір даних:
   - Flask/FastAPI REST API
   - Приймає дані від агентів
   - Зберігає в базу даних
   - Надає web інтерфейс для перегляду
   - API для запитів даних

3. **CLI клієнт (`client.py`)** - інтерфейс для адміністратора:
   ```bash
   python client.py list-hosts
   python client.py show-metrics --host server1
   python client.py alerts --last-day
   python client.py stats --host server1 --metric cpu
   ```

4. **Web Dashboard** - візуалізація в браузері:
   - Список всіх моніторених хостів
   - Графіки метрик в реальному часі
   - Історія попереджень
   - Налаштування порогів

**API Endpoints:**
```
POST   /api/v1/metrics          - прийняти метрики від агента
GET    /api/v1/hosts            - список хостів
GET    /api/v1/metrics/:host    - метрики для хоста
GET    /api/v1/alerts           - список попереджень
```

**Технології:**
- Flask або FastAPI для API
- SQLite або PostgreSQL для БД
- Chart.js або Plotly для графіків в web
- Bootstrap для UI

**Бонуси:**
- Аутентифікація (токени для агентів)
- Шифрування трафіку (HTTPS)
- Масштабованість (підтримка багатьох агентів)
- Containerization (Docker)
- Alerting через Telegram Bot

---

## 📊 Критерії оцінювання

### Базові завдання (1 бал кожне):
- ✅ Код виконується без помилок
- ✅ Виконує заявлену функціональність
- ✅ Має базову обробку помилок

### Середні завдання (2 бали кожне):
- ✅ Всі критерії базового рівня
- ✅ Має docstrings та коментарі
- ✅ Використовує type hints
- ✅ Має валідацію введених даних

### Складні завдання (3 бали кожне):
- ✅ Всі критерії середнього рівня
- ✅ Має повну обробку помилок
- ✅ Код добре структурований (класи, функції)
- ✅ Має юніт-тести (опціонально, +0.5 бали)
- ✅ Має README з описом та прикладами (опціонально, +0.5 бали)

---

## 🎯 Рекомендації до виконання

### Як починати:
1. Спочатку прочитайте всю умову завдання
2. Розбийте завдання на підзадачі
3. Напишіть псевдокод або план
4. Реалізуйте функціональність покроково
5. Тестуйте кожну частину окремо
6. Додайте обробку помилок
7. Додайте документацію

### Структура проекту:
```
monitoring_project/
├── basic/
│   ├── cpu_monitor.py
│   ├── memory_info.py
│   └── ...
├── intermediate/
│   ├── top_processes.py
│   ├── system_report_json.py
│   └── ...
├── advanced/
│   ├── advanced_monitor.py
│   ├── process_analyzer.py
│   └── ...
├── requirements.txt
└── README.md
```

### requirements.txt:
```
psutil>=5.9.0
colorama>=0.4.6  # для кольорового виводу
matplotlib>=3.7.0  # для графіків (опціонально)
flask>=2.3.0  # для web dashboard (опціонально)
```

---

## 🏆 Бонусні завдання (+1 бал кожне)

### Бонус 1: Кольоровий вивід
Додайте красивий кольоровий вивід з використанням бібліотеки `rich`:
```python
from rich.console import Console
from rich.table import Table
from rich.progress import Progress

console = Console()
console.print("[bold green]✓[/bold green] Система в нормі")
```

### Бонус 2: Progress bars
Додайте візуальні progress bar для відображення завантаження:
```python
from rich.progress import track

for i in track(range(100), description="Processing..."):
    # робота
```

### Бонус 3: Logging замість print
Використовуйте модуль `logging` замість `print()`:
```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s'
)

logger = logging.getLogger(__name__)
logger.info("Моніторинг запущено")
```

### Бонус 4: Конфігураційний файл
Читання налаштувань з YAML або TOML файлу:
```yaml
# config.yaml
thresholds:
  cpu: 80
  memory: 90
  disk: 95

monitoring:
  interval: 60
  output_dir: ./reports
```

### Бонус 5: Systemd service
Створіть systemd service файл для автоматичного запуску:
```ini
# /etc/systemd/system/system-monitor.service
[Unit]
Description=System Monitoring Service
After=network.target

[Service]
Type=simple
User=monitoring
ExecStart=/usr/bin/python3 /opt/monitoring/advanced_monitor.py --continuous
Restart=always

[Install]
WantedBy=multi-user.target
```

---

## 📚 Корисні ресурси

1. **Документація psutil:**
   - https://psutil.readthedocs.io/en/latest/

2. **Документація argparse:**
   - https://docs.python.org/3/library/argparse.html

3. **Rich library (красивий вивід):**
   - https://rich.readthedocs.io/

4. **SQLite в Python:**
   - https://docs.python.org/3/library/sqlite3.html

5. **Matplotlib (графіки):**
   - https://matplotlib.org/stable/tutorials/index.html

6. **Flask (Web API):**
   - https://flask.palletsprojects.com/

---

## ❓ FAQ

**Q: Як перевірити чи процес існує?**
```python
import psutil
try:
    proc = psutil.Process(pid)
    print(f"Процес {pid} існує")
except psutil.NoSuchProcess:
    print(f"Процес {pid} не існує")
```

**Q: Як обробити Ctrl+C в Python?**
```python
try:
    while True:
        # робота
        time.sleep(1)
except KeyboardInterrupt:
    print("\n✅ Зупинено користувачем")
```

**Q: Як очистити екран в терміналі?**
```python
import os
import platform

def clear_screen():
    os.system('cls' if platform.system() == 'Windows' else 'clear')
```

**Q: Як форматувати розмір в байтах до GB?**
```python
def format_bytes(bytes_num):
    for unit in ['B', 'KB', 'MB', 'GB', 'TB']:
        if bytes_num < 1024.0:
            return f"{bytes_num:.2f} {unit}"
        bytes_num /= 1024.0
```

---

**Успіхів у виконанні завдань! 🚀**

Пам'ятайте: найкращий спосіб навчитись — це практика. Не бійтесь експериментувати та робити помилки!