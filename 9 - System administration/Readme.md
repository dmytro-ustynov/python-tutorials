
## 🎓 **ЗАНЯТТЯ 5.1. Системне програмування в Python**

### 🕐 Загальний час: 2 години (лекція) + 0.75 год самостійна робота

### 🎯 **Мета:**

Показати, як Python взаємодіє з операційною системою: робота з процесами, змінними середовища, системними ресурсами та виконання зовнішніх команд.

---

## 🧩 СТРУКТУРА СЛАЙДІВ

---

### **Слайд 1. Заголовок**

**Тема:** Системне програмування в Python
**План заняття:**

1. Модулі `os` та `sys`
2. Запуск зовнішніх команд через `subprocess`
3. Моніторинг системи з `psutil`
   **Самостійна робота:** Кросплатформна сумісність

---

### **Слайд 2. Що таке системне програмування**

* Це взаємодія коду з ресурсами ОС: файлами, процесами, пам’яттю, користувачами, змінними середовища.
* Python має потужні стандартні модулі для цього: `os`, `sys`, `subprocess`, `psutil`.
* Сценарії системного адміністрування часто будуються на Python завдяки кросплатформності.

---

## 🔹 ЧАСТИНА 1. Модулі `os` та `sys`

---

### **Слайд 3. Модуль `os`: доступ до функцій ОС**

```python
import os

print(os.name)           # 'posix' або 'nt'
print(os.getcwd())       # Поточний каталог
print(os.listdir('.'))   # Список файлів
```

🔹 **Основні функції:**

* `os.getcwd()`, `os.chdir()` — робота з каталогами
* `os.mkdir()`, `os.remove()`, `os.rename()` — операції з файлами
* `os.environ` — змінні середовища

---

### **Слайд 4. Робота зі змінними середовища**

```python
import os

print(os.environ.get("PATH"))
os.environ["MY_APP_MODE"] = "production"
```

🧠 **Для чого:**

* Зберігання налаштувань без “хардкодингу”.
* Безпечне зчитування токенів або паролів (через `.env`).

---

### **Слайд 5. Модуль `sys`**

```python
import sys

print(sys.platform)       # Назва ОС
print(sys.version)        # Версія Python
print(sys.argv)           # Аргументи командного рядка
sys.exit(0)
```

🔹 Використання:

* обробка аргументів при запуску скриптів
* контроль завершення програми
* доступ до стандартних потоків (`sys.stdin`, `sys.stdout`, `sys.stderr`)

---

## 🔹 ЧАСТИНА 2. Запуск зовнішніх команд (`subprocess`)

---

### **Слайд 6. Виконання команд системи**

```python
import subprocess

result = subprocess.run(["ping", "-c", "3", "google.com"], capture_output=True, text=True)
print(result.stdout)
```

🧩 **Пояснення:**

* `run()` — найзручніший сучасний API.
* `capture_output=True` → зчитати stdout/stderr.
* `text=True` → автоматичне декодування у str.

---

### **Слайд 7. Приклади використання**

```python
import subprocess

# Вивести список процесів (Linux)
subprocess.run(["ps", "aux"])

# Перевірити існування користувача
user = "admin"
result = subprocess.run(["id", user], capture_output=True)
print("OK" if result.returncode == 0 else "No such user")
```

📘 **Зауваження з безпеки:**

* Не використовуйте `shell=True` без потреби → ризик ін’єкцій.
* Краще передавати список аргументів, а не єдиний рядок.


```python
import subprocess

# Створення нового процесу за допомогою Popen
process = subprocess.Popen(['ping', '8.8.8.8'], stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True)

# Очікування завершення процесу та отримання виводу
stdout, stderr = process.communicate()

print(f"Вивід PING:\n{stdout}")
print(f"Код завершення: {process.returncode}")
```

```python
import subprocess

# Запуск команди 'ls -l' (Linux/macOS) або 'dir' (Windows)
try:
    result = subprocess.run(["ls", "-l"], check=True)
    print("Команда виконана успішно.")
except subprocess.CalledProcessError as e:
    print(f"Помилка виконання команди: {e}")
```

---

## 🔹 ЧАСТИНА 3. Моніторинг системних ресурсів (`psutil`)

---

### **Слайд 8. Модуль `psutil` — потужний інструмент**

```python
import psutil

print(psutil.cpu_percent(interval=1))
print(psutil.virtual_memory())
print(psutil.disk_usage('/'))
```

🔹 **Можливості:**

* CPU, RAM, диски, мережа
* Перелік процесів
* Інформація про користувачів і uptime


```python
import psutil

memory = psutil.virtual_memory()

print(f"Всього пам'яті: {memory.total / (1024**3):.2f} ГБ")
print(f"Доступно пам'яті: {memory.available / (1024**3):.2f} ГБ")
print(f"Використано пам'яті: {memory.used / (1024**3):.2f} ГБ")
print(f"Відсоток використання: {memory.percent}%")
```

---

### **Слайд 9. Приклад: Моніторинг процесів**

```python
import psutil

for proc in psutil.process_iter(['pid', 'name', 'username']):
    print(proc.info)
```

📊 **Застосування:**

* системний моніторинг
* створення власних “health checks”
* аналітика навантаження

```python
import psutil

# Отримати використання ЦП за останні 2 секунди
cpu_percent = psutil.cpu_percent(interval=2)
print(f"Використання ЦП: {cpu_percent}%")

# Отримати статистику по кожному ядру окремо
cpu_per_cpu = psutil.cpu_percent(interval=1, percpu=True)
print(f"Використання ЦП по ядрах: {cpu_per_cpu}")
```


```python
import psutil

memory = psutil.virtual_memory()

print(f"Всього пам'яті: {memory.total / (1024**3):.2f} ГБ")
print(f"Доступно пам'яті: {memory.available / (1024**3):.2f} ГБ")
print(f"Використано пам'яті: {memory.used / (1024**3):.2f} ГБ")
print(f"Відсоток використання: {memory.percent}%")
```
---

### **Слайд 10. Зв’язок з кібербезпекою**

* Виявлення підозрілих процесів (анти-malware інструменти).
* Автоматичний аудит логів і процесів.
* Системи раннього сповіщення (ECA/IDS).

---

### **Слайд 11. Приклад комплексного скрипта**

```python
import os, psutil, datetime

report = {
    "time": datetime.datetime.now().isoformat(),
    "user": os.getenv("USER") or os.getenv("USERNAME"),
    "cpu": psutil.cpu_percent(),
    "ram": psutil.virtual_memory().percent,
}

print(report)
```

📈 Можна записати цей звіт у JSON або лог — основа для моніторингу.

---

## 🔹 Самостійна робота: “Кросплатформна сумісність”

---

### **Слайд 12. Основні відмінності**

* ОС Linux, macOS, Windows — різні команди та шляхи (`/` vs `\`).
* Використовуйте `os.path`, `pathlib`, `platform`.
* Не покладайтеся на зовнішні команди — перевіряйте через Python API.

---

### **Слайд 13. Приклад: універсальний шлях**

```python
from pathlib import Path

path = Path.home() / "Documents" / "report.txt"
print(path)
```

```python
import os
from pathlib import Path

# Використання os.path:
# os.path.join() автоматично вибере потрібний роздільник
file_path_os = os.path.join("директорія", "піддиректорія", "файл.txt")
# На Windows: 'директорія\піддиректорія\файл.txt'
# На Linux: 'директорія/піддиректорія/файл.txt'

# Використання pathlib (сучасний підхід):
file_path_pathlib = Path("директорія") / "піддиректорія" / "файл.txt"
# Результат аналогічний os.path.join()
```

Приклад -- різні команди в різних системах

```python
import subprocess
import platform

if platform.system() == "Windows":
    # Команда для Windows
    subprocess.run(["dir"])
else:
    # Команда для Linux/macOS
    subprocess.run(["ls", "-l"])
```


```python
import os
from pathlib import Path

# os.path.expanduser() знаходить домашню директорію кросплатформно
home_dir_os = os.path.expanduser("~")

# Path.home() робить те саме
home_dir_path = Path.home()
```
---

### **Слайд 14. Підсумок лекції**

✅ Робота з ОС через `os`, `sys`
✅ Виконання системних команд через `subprocess`
✅ Моніторинг системи з `psutil`
✅ Важливість безпеки й кросплатформності

---

## 🧭 **Далі (заняття 2):**

“Автоматизація файлових операцій” — робота з `pathlib`, копіювання, архівування, резервне копіювання.

