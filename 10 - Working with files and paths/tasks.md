# 📝 Практичні завдання: Робота з файловими шляхами та пошук файлів

---

## 📋 Структура завдань

**Частина 1: Базові завдання** (обов'язкові для всіх) — 40 балів
- Кожен студент виконує всі базові завдання
- Ці завдання перевіряють основні навички

**Частина 2: Варіативні завдання** (за номером у журналі) — 6 балів
- Кожному студенту призначається варіант за формулою: `номер_у_журналі % 6 + 1`
- Всі варіанти однакової складності, але працюють з різними типами файлів

**Максимум:** 1.0 бал

---

## ⚙️ Підготовка

```bash
mkdir lesson_10_tasks
cd lesson_10_tasks
```

---

# 📌 ЧАСТИНА 1: БАЗОВІ ЗАВДАННЯ (обов'язкові для всіх)

---

## Завдання 1.1: Основи pathlib (1.0 бал)

**Мета:** Навчитися працювати з Path об'єктами.

**Завдання:**
Напишіть програму `path_basics.py`, яка:

1. Виводить абсолютний шлях до домашньої директорії
2. Виводить абсолютний шлях до поточної робочої директорії
3. Створює структуру директорій: `test_project/src/modules/`
4. Створює файл `test_project/README.md` з текстом "Test Project"
5. Виводить інформацію про створений файл:
   - Повний шлях
   - Ім'я файлу
   - Розширення
   - Батьківська директорія
   - Розмір у байтах

**Приклад виводу:**
```
Домашня директорія: /home/user
Поточна директорія: /home/user/lesson_10_tasks

Створено структуру: test_project/src/modules/
Створено файл: test_project/README.md

Інформація про файл:
  Повний шлях: /home/user/lesson_10_tasks/test_project/README.md
  Ім'я: README.md
  Розширення: .md
  Батьківська директорія: test_project
  Розмір: 13 bytes
```

---

## Завдання 1.2: Властивості файлів (1.5 бали)

**Мета:** Навчитися читати та працювати з часовими мітками файлів.

**Завдання:**
Напишіть програму `file_properties.py`, яка:

1. Створює 3 тестові файли з затримкою 2 секунди між створенням
2. Для кожного файлу виводить:
   - Час створення/зміни (mtime)
   - Час останнього доступу (atime)
   - Розмір файлу
3. Сортує файли за часом створення (від найстарішого до найновішого)
4. Виводить, скільки секунд пройшло з моменту створення кожного файлу

**Підказка:**
```python
import time
from pathlib import Path
import datetime

# Створення файлу з затримкою
Path("file1.txt").write_text("Content 1")
time.sleep(2)
Path("file2.txt").write_text("Content 2")
```

---

## Завдання 1.3: Пошук за патерном (1.5 бали)

**Мета:** Навчитися використовувати glob та rglob для пошуку файлів.

**Завдання:**
Напишіть програму `file_search.py`, яка:

1. Створює тестову структуру:
   ```
   test_data/
     ├── documents/
     │   ├── report.txt
     │   ├── data.csv
     │   └── notes.txt
     ├── images/
     │   ├── photo1.jpg
     │   └── photo2.png
     └── code/
         ├── main.py
         └── utils.py
   ```

2. Шукає та виводить:
   - Всі `.txt` файли (рекурсивно)
   - Всі `.py` файли
   - Всі файли в директорії `images/`
   - Загальна кількість файлів у всій структурі

**Приклад виводу:**
```
Текстові файли (.txt):
  test_data/documents/report.txt
  test_data/documents/notes.txt

Python файли (.py):
  test_data/code/main.py
  test_data/code/utils.py

Файли в images/:
  test_data/images/photo1.jpg
  test_data/images/photo2.png

Всього файлів: 7
```

---

# 📌 ЧАСТИНА 2: ВАРІАТИВНІ ЗАВДАННЯ (за номером у журналі)

**Як визначити свій варіант:**
```python
MY_JOURNAL_NUMBER = 15  # Замініть на ваш номер у журналі
my_option = (MY_JOURNAL_NUMBER % 6) + 1
print(f'Мій варіант завдання: {my_option}')
```


---

## 🔵 Варіант 1: Аналізатор медіа-файлів (6 балів)

**Мета:** Аналіз колекції фото/відео файлів.

**Завдання:**
Створіть програму `media_analyzer.py`, яка аналізує директорію з медіа-файлами.

**Функціональність:**

1. **Сканування** (1.5 бали)
   - Рекурсивний пошук файлів: `.jpg`, `.jpeg`, `.png`, `.gif`, `.mp4`, `.avi`, `.mov`
   - Підрахунок кількості файлів кожного типу
   - Загальний розмір кожного типу

2. **Аналіз за розміром** (1.5 бали)
   - Найбільший файл (ім'я, розмір у MB)
   - Найменший файл
   - Середній розмір файлу
   - Розподіл за категоріями:
     - Маленькі (< 1 MB)
     - Середні (1-10 MB)
     - Великі (> 10 MB)

3. **Аналіз за датою** (1.5 бали)
   - 10 найновіших файлів (за mtime)
   - 10 найстаріших файлів
   - Файли, створені за останній місяць

4. **Звіт** (1.5 бали)
   - Вивід результатів у консоль у форматованому вигляді
   - Збереження звіту у JSON файл `media_report.json`

**Приклад виводу:**
```
=== АНАЛІЗ МЕДІА-ФАЙЛІВ ===
Директорія: /home/user/Photos

Файли за типом:
  JPG:  150 файлів, 450.5 MB
  PNG:  45 файлів, 120.3 MB
  MP4:  20 файлів, 2.5 GB

Найбільший файл: vacation_video.mp4 (350 MB)
Найменший файл: thumbnail.jpg (15 KB)
Середній розмір: 12.5 MB

Розподіл:
  Маленькі (< 1 MB):    80 файлів
  Середні (1-10 MB):    100 файлів
  Великі (> 10 MB):     35 файлів

Файли за останній місяць: 25 файлів
```

---

## 🟢 Варіант 2: Аналізатор документів (6 балів)

**Мета:** Аналіз колекції текстових документів.

**Завдання:**
Створіть програму `document_analyzer.py`, яка аналізує директорію з документами.

**Функціональність:**

1. **Сканування** (1.5 бали)
   - Пошук файлів: `.txt`, `.md`, `.pdf`, `.doc`, `.docx`, `.odt`
   - Підрахунок кількості кожного типу
   - Загальний розмір

2. **Аналіз текстових файлів** (2.0 бали)
   - Для `.txt` та `.md` файлів:
     - Кількість рядків
     - Кількість слів
     - Топ-5 найдовших файлів (за кількістю рядків)
   - Кодування файлів (UTF-8, ASCII, інше)

3. **Пошук за вмістом** (1.5 бали)
   - Пошук файлів, що містять задане слово/фразу
   - Список файлів з найчастішою зустрічальністю ключового слова
   - Файли без вмісту (порожні)

4. **Звіт** (1.0 бал)
   - Форматований вивід у консоль
   - Експорт у JSON: `document_report.json`

**Приклад виводу:**
```
=== АНАЛІЗ ДОКУМЕНТІВ ===
Директорія: /home/user/Documents

Типи файлів:
  TXT:  80 файлів, 15.2 MB
  MD:   25 файлів, 3.5 MB
  PDF:  40 файлів, 120 MB

Аналіз текстових файлів:
  Всього рядків: 45,000
  Всього слів: 320,000

Топ-5 найбільших файлів:
  1. thesis.txt (5,200 рядків)
  2. notes_2024.md (3,800 рядків)
  3. book_draft.txt (2,900 рядків)

Пошук слова "Python": 15 файлів

Порожні файли: 3
```

---

## 🟡 Варіант 3: Аналізатор вихідного коду (6 балів)

**Мета:** Аналіз проєкту з програмним кодом.

**Завдання:**
Створіть програму `code_analyzer.py`, яка аналізує директорію з кодом.

**Функціональність:**

1. **Сканування** (1.5 бали)
   - Пошук файлів: `.py`, `.js`, `.java`, `.cpp`, `.c`, `.h`
   - Кількість файлів кожної мови
   - Ігнорування: `__pycache__`, `node_modules`, `.git`

2. **Аналіз Python файлів** (2.0 бали)
   - Підрахунок рядків коду (без порожніх та коментарів)
   - Кількість класів (рядки, що починаються з `class `)
   - Кількість функцій (`def `)
   - Топ-5 найбільших файлів (за кількістю рядків)

3. **Статистика проєкту** (1.5 бали)
   - Загальна кількість рядків коду
   - Співвідношення код/коментарі
   - Найдовший файл
   - Файли без жодної функції

4. **Звіт** (1.0 бал)
   - Консольний вивід
   - JSON звіт: `code_report.json`

**Приклад виводу:**
```
=== АНАЛІЗ КОДУ ===
Проєкт: /home/user/myproject

Файли за мовами:
  Python: 45 файлів
  JavaScript: 20 файлів
  C++: 10 файлів

Аналіз Python:
  Рядків коду: 8,500
  Класів: 25
  Функцій: 180

Топ-5 файлів:
  1. main.py (850 рядків)
  2. models.py (620 рядків)
  3. utils.py (480 рядків)

Середній розмір файлу: 189 рядків
```

---

## 🟣 Варіант 4: Аналізатор конфігураційних файлів (6 балів)

**Мета:** Аналіз конфігурацій системи/додатків.

**Завдання:**
Створіть програму `config_analyzer.py`, яка аналізує конфігураційні файли.

**Функціональність:**

1. **Сканування** (1.5 бали)
   - Пошук файлів: `.json`, `.yaml`, `.yml`, `.ini`, `.conf`, `.xml`, `.env`
   - Кількість файлів кожного типу
   - Загальний розмір

2. **Аналіз JSON файлів** (2.0 бали)
   - Валідність (чи можна розпарсити)
   - Кількість ключів верхнього рівня
   - Глибина вкладеності
   - Файли з помилками синтаксису

3. **Пошук чутливих даних** (1.5 бали)
   - Файли, що містять слова: `password`, `secret`, `api_key`, `token`
   - Вивести попередження для кожного знайденого файлу
   - Список файлів з потенційно небезпечною інформацією

4. **Звіт** (1.0 бал)
   - Консольний вивід
   - JSON звіт: `config_report.json`

**Приклад виводу:**
```
=== АНАЛІЗ КОНФІГУРАЦІЙ ===
Директорія: /etc/myapp

Типи файлів:
  JSON: 15 файлів
  YAML: 8 файлів
  INI:  5 файлів
  ENV:  3 файлів

Аналіз JSON:
  Валідні: 14 файлів
  Помилки: 1 файл (settings_old.json)

⚠️ ПОПЕРЕДЖЕННЯ: Знайдено чутливі дані!
  config.json: містить "api_key"
  database.yaml: містить "password"
  secrets.env: містить "token"

Всього файлів з чутливими даними: 3
```

---

## 🔴 Варіант 5: Аналізатор лог-файлів (6 балів)

**Мета:** Аналіз системних та application логів.

**Завдання:**
Створіть програму `log_analyzer.py`, яка аналізує лог-файли.

**Функціональність:**

1. **Сканування** (1.5 бали)
   - Пошук файлів: `.log`, `.log.1`, `.log.gz`
   - Кількість файлів
   - Загальний розмір (включно зі стиснутими)

2. **Аналіз вмісту** (2.0 бали)
   - Підрахунок рядків у кожному лозі
   - Пошук рівнів логування: `ERROR`, `WARNING`, `INFO`, `DEBUG`
   - Файли з найбільшою кількістю помилок (ERROR)
   - Часовий діапазон (перший та останній timestamp у логах)

3. **Пошук проблем** (1.5 бали)
   - Файли з помилками за останню годину
   - Топ-5 найчастіших помилок (якщо є шаблони)
   - Логи, які не оновлювалися більше 7 днів

4. **Звіт** (1.0 бал)
   - Консольний вивід
   - JSON звіт: `log_report.json`

**Приклад виводу:**
```
=== АНАЛІЗ ЛОГІВ ===
Директорія: /var/log/myapp

Лог-файли:
  Активні (.log): 5 файлів, 250 MB
  Архівні (.gz):  15 файлів, 80 MB

Статистика:
  Всього рядків: 1,250,000
  ERROR:   850
  WARNING: 3,200
  INFO:    1,245,000

Файли з найбільшою кількістю помилок:
  1. error.log (650 помилок)
  2. application.log (150 помилок)

⚠️ Помилки за останню годину: 15

Застарілі логи (> 7 днів): 3 файли
```

---

## 🟠 Варіант 6: Аналізатор файлів даних (6 балів)

**Мета:** Аналіз датасетів та файлів даних.

**Завдання:**
Створіть програму `data_analyzer.py`, яка аналізує файли з даними.

**Функціональність:**

1. **Сканування** (1.5 бали)
   - Пошук файлів: `.csv`, `.tsv`, `.json`, `.xml`, `.sqlite`, `.db`
   - Кількість файлів кожного типу
   - Загальний розмір

2. **Аналіз CSV файлів** (2.0 бали)
   - Кількість рядків (без заголовка)
   - Кількість колонок
   - Перші 3 колонки (назви)
   - Файли з некоректною структурою

3. **Статистика даних** (1.5 бали)
   - Найбільший файл даних
   - Файли, що містять певний термін у заголовках (наприклад, "date", "email")
   - Порожні файли даних

4. **Звіт** (1.0 бал)
   - Консольний вивід
   - JSON звіт: `data_report.json`

**Приклад виводу:**
```
=== АНАЛІЗ ФАЙЛІВ ДАНИХ ===
Директорія: /home/user/datasets

Типи файлів:
  CSV:    25 файлів, 450 MB
  JSON:   10 файлів, 120 MB
  SQLite: 3 файли, 80 MB

Аналіз CSV:
  users.csv: 50,000 рядків, 8 колонок
  transactions.csv: 120,000 рядків, 12 колонок
  products.csv: 5,000 рядків, 15 колонок

Найбільший файл: sales_2024.csv (180 MB, 500,000 рядків)

Файли з email колонкою: 5 файлів
Файли з date колонкою: 18 файлів

Порожні файли: 2
```

---

## 🎯 Критерії оцінювання

### Базові завдання (40 балів):
- Завдання 1.1: 1.0 бал
- Завдання 1.2: 1.5 бали
- Завдання 1.3: 1.5 бали

### Варіативне завдання (6 балів):
- Сканування та підрахунок: 1.5 бали
- Специфічний аналіз: 2.0 бали
- Додаткова функціональність: 1.5 бали
- Звіт та форматування: 1.0 бал

**Всього:** 100 балів

---

## 💡 Загальні вимоги до коду

1. **Використання pathlib** обов'язкове (не os.path)
2. **Обробка помилок** для всіх файлових операцій
3. **Читабельність коду**: коментарі, зрозумілі назви змінних
4. **Форматований вивід**: таблиці, вирівнювання
5. **JSON звіт** має бути валідним

**Приклад обробки помилок:**
```python
from pathlib import Path

try:
    file_path = Path("file.txt")
    content = file_path.read_text(encoding='utf-8')
except FileNotFoundError:
    print(f"Файл не знайдено: {file_path}")
except PermissionError:
    print(f"Немає доступу: {file_path}")
except Exception as e:
    print(f"Помилка: {e}")
```

---

## 📚 Корисні підказки

### Підрахунок рядків у файлі:
```python
line_count = len(Path("file.txt").read_text().splitlines())
```

### Перевірка JSON валідності:
```python
import json

try:
    with open("config.json") as f:
        data = json.load(f)
    print("Валідний JSON")
except json.JSONDecodeError:
    print("Помилка синтаксису JSON")
```

### Форматований вивід:
```python
print(f"{'Назва':<30} {'Розмір':>15} {'Дата':<20}")
print("-" * 65)
print(f"{'report.txt':<30} {'2.5 MB':>15} {'2024-11-09':<20}")
```

### Збереження у JSON:
```python
import json

report = {
    "total_files": 150,
    "total_size_mb": 450.5,
    "file_types": {"jpg": 100, "png": 50}
}

with open("report.json", 'w', encoding='utf-8') as f:
    json.dump(report, f, indent=2, ensure_ascii=False)
```

---

**Успіхів у виконанні завдань! 🚀**